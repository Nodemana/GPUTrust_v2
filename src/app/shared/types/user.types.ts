export interface User {
    id: string;
    name: string;
    email: string;
    solanaAddress: string;
    password: string;
}

export interface UserCardData {
    id: string;
    name: string;
    email: string;
}